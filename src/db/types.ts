type Notes = {
    id: string,
    phone: string,
    asset: string,
    email: string,
    description: string,
    ipv4: string
}

type CheckList = {
    id: string;
    title: string;
    questions: string;
    isTemplate: boolean
    template: string
}